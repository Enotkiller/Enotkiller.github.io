# 00Объяснение кода понятными словами.

---

Есть 3 файла [`main.py`](main.py), [`data.py`](data.py), [`date_for_now.py`](date_for_now.py).

В [`main.py`](data.py) использует функции для вычесление какая сейчас пара из файла [`data.py`](data.py).

А [`data.py`](data.py) просто обобщает, сам по себе он ничего не вычисляет, просто приводит в кучу все базовые и раздробленые функции из [`date_for_now.py`](date_for_now.py).

И ещё в [`data.py`](data.py) хранится расписание пар, ссылки на эти пары, и ещё пару вещей для вычисление тип недели, оменили ли пару, и всё.

и самый последний файл это [`date_for_now.py`](date_for_now.py) в нём как раз и происходит вычисление вся нам нужная информация, это: какая сейчас пара по номеру

какая именно пара (название), тип недели, время по utc. И начнем разбор именно с этого файлика. :blush:

Мы возьмем все функции и каждую по отдельности разберём.

![фотка на функции](https://i.imgur.com/gI3G4cB.png)

---

Первая `date_return` (я ещё в каждую фунцию коменнтарии добавил).

```python
def date_return(self):
    res = datetime.datetime.now()
    return int(res.isoweekday())
```

Функция возвращает сегоднешний день недели:


| **День недели** | **Вывод** |
| ------------------------- | -------------- |
| Понедельник    | 1              |
| Вторник            | 2              |
| Среда                | 3              |
| Четверг            | 4              |
| Пятница            | 5              |

Реализовано очень просто получаем сегодняшний день, и через функцию `isoweekday` получаем день недели, если убрать в начале `is`,

то получим функцию `weekday()` и она будет возвращать всё тоже самое только результат будет на **1** меньше.

---

Следующая функция это `full_date_return`.

```python
def full_date_return(self, _now = 0, day = 10, mounth = 2):
    cogda_mounth, cogda_day = mounth, day
    mounth = int(datetime.datetime.now().strftime("%m"))
    day = int(datetime.datetime.now().strftime("%d"))
    year = int(datetime.datetime.now().strftime("20%y"))
    now = int(1) if _now == 0 else int(0)
    for i in range(mounth - cogda_mounth + 1):
        for j in range(1, calendar.monthrange(year, i + cogda_mounth)[1] + 1):
            if i == 0 and j <= cogda_day - 1:
                pass
            else:
                if self.date_for_weekly(cogda_mounth + i, j) == 1:
                    if now == 0:
                        now = 1
                    else:
                        now = 0
                if i + cogda_mounth == mounth and j >= day:
                    break
    return now
```

Для неё импортируется библеотека `calendar` по позже объясню зачем.

Начнем с начало, это функция возвращает какой сейчас тип недели (численный, знаменатель).

в начале мы получаем 3 параметра:


| **Ввод** | **Обозначение**                                      |
| ------------ | --------------------------------------------------------------- |
| `_now`       | Тип недели от которой идёт отчёт     |
| `day`        | Дата недели от которой идёт отчёт   |
| `mounth`     | Месяц недели от которой идёт отчёт |

Если не вникая объяснить то это функция считает от определенной даты до нынешней понедельники, и при каждом пройдёном понедельником,

он меняет значение `now` на обратное.

В `now` хранится тип недели:


| **Значение** | **Обозначение**                  |
| -------------------- | ------------------------------------------- |
| 1                    | Числитель (чисельник)     |
| 0                    | Знаменитель (знаменник) |

Дальше у нас есть 2 цикла и как они работают мне тяжело объяснить но могу примерно и привести примеры.

Вот разница в месяцах между датой которой мы задавали и нынешней датой, мы используем в первом цикле. Вот сейчас у мы отсчитываем от 10 02,

а сейчас 31 03, разница в 1 месяц но мы плюсуем ещё 1 так как нам надо посчитать ещё месяц с той датой которой мы задали в функцию.

Вот этот выделен этот цикл:

![Фотка на первый цикл](https://i.imgur.com/czy3mok.png)

И теперь мы переходим к вложеному циклу, а он будет делать обороты (я буду так обозначать то сколько раз будет выполнять код в цикле)

столько сколько дней в месяце, пример:

Так как у нас в прошлом примере мы дату передали 10 02 и сейчас 31 03, то и здесь мы их будем использовать.

Грубо но чётко можно объяснить так мы считаем каждую неделю в промежутке между датами, а именно мы считаем понедельники,

и так как первый цикл нам даёт `i`, а именно "разницу в месяцу", то мы с начало посчитаем с значением `i = 0`.

Но что бы получать именно последущии месяца между датами мы будем `i` плюсовать с месяцом которым мы передали в функцию,

а в примере мы считаем с 02 месяца (февраль), то `2 + 0 = 2` и с помощью библиотеки которую я упомянул ранее (`calendar`)

мы получить количество дней в фефрале месяце этого года, и потом так как ведь мы считаем не с 1 числа, а с 10.

То мы в начале проверяем на то что `i == 0` и на то что `j <= задаваемого дня` (`j` это то что "возвращает" второй цикл, но так не правильно говорить).

Вот и пока мы не до считаем до дня котого мы задали мы все будем пасовать.

И как только мы доходим до этого дня, мы просто отсчитываем каждый понедельник с помощью следущей функции `date_for_weekly`

мы получаем день недели определенной даты которую мы передадим и меняем `now` на обратное значение.

И вот мы прошли первый 02 (февраль) и всё цикл закончился, но так как разница и мы плюсовали 1 то у нас цикл ещё сделает оборот,

так как `разница = 1` и `+1` то выйдет 2 оборота. И так как `i` теперь ровна 1, то в 2 цикле при получении количество дней

в месяце, месяц он будет передать определять все тем же способом, это берем изначально переданный функции месяц и + `i`,

то у нас выйдет `02 + 1 = 03` (03 март), то мы теперь ещё считаем и понедельники марта, и так совпало что март это и последний месяц,

то нам надо сделать проверку на то что бы наша дата в цикле не перешагнула на последующие даты в этом месяц за нынешнею дату.

То как выходим из циклов.

![фотка с выходом из циклов](https://i.imgur.com/W8oBhob.png)

И просто возращаем `now` который вышел по результату данных головоломак.

---

Следующая функция `date_for_weekly`, та которую мы использовали в прошлой функции.

```python
def date_for_weekly(self, mounth, day):
    year = int(datetime.datetime.now().strftime("20%y"))
    date = datetime.date(year, mounth, day)
    return date.isoweekday()
```

Мы в неё передеаем дату, и на выходе получаем день недели этой даты.

Вот 01 04 (1 апреля 25) то это будет вторник и он вернет 2.

Разница между этой функцией и `date_return` в том что это возвращает день недели переданой даты в функцию,

а `date_return` возвращает именно какой сегодня день недели.

---

Следующая функция `time_return`.

Она простенькая, она возвращает время в `str` например `"21:20"`, только считает его по utc.

```python
def time_return(self):
    utc_now = datetime.datetime.now(datetime.timezone.utc)
    utc_plus_2 = utc_now + datetime.timedelta(hours=3)
    time = utc_plus_2.strftime("%H:%M")
    return time
```

---

Следующая функция `quest_return`

```python
def quest_return(self, _time):
    data = _time.split(":")
    time = float(f"{data[0]}.{data[1]}")
    if time >= 8.0 and time <= 9.49:
        if time >= 8.0 and time <= 8.30:
            return -1
        else:
            return 1
    elif time >= 9.50 and time <= 11.20:
        if time >= 9.50 and time <= 9.59:
            return -2
        else:
            return 2
    elif time >= 11.20 and time <= 13.20:
        if time >= 11.20 and time <= 11.59:
            return -3
        else:
            return 3
    elif time >= 13.20 and time <= 14.50:
        if time >= 13.20 and time <= 13.29:
            return -4
        else:
            return 4
    return 0
```

Она простенькая, суть её это когда ты передаешь ей время в `str`, а она через `split` получаем их в `float` = `"21:10" = 21.10`.

А дальше всё просто, если время в промежутке с 8 по 9.49 то это 100% первая пара, но у меня ещё внутри ещё условие на то что если

время в промежутке с 8 по 8.30, то это означает что сейчас перемена но следующая пара все же первая то возвращаем `-1`, а если не перемена

то возвращаем `1`, и так с каждой парой.

---

Следующая функция это `perema_now_return` (должно быть написано перемена, но написано перема, пусть так и останется для мой несовершенности)

```python
def perema_now_return(self, quest):
    if quest < 0:
        return True
    else:
        return False
```

Функция просто проверяет если результат от прошлой функции **минусовое** то вернет `True`, если нет то `False`.

---

Последняя функция в этом файле это `para_return`.

```python
def para_return(self, data, quest, cal, full_date):
    parameters = None
    if data <= 5 and quest != 0:
        for i in range(1, 4 + 1):
            if quest == i or quest == i * -1:
                if full_date == 1:
                    parameters = cal.get(data).get(i)
                else:
                    if cal.get(data).get(float(f"{i}.5")) is None:
                        parameters = cal.get(data).get(i)
                    else:
                        parameters = cal.get(data).get(float(f"{i}.5"))

        if parameters != None:
            return parameters
        else:
            return "Пары нет"
    else:
        return "Выходные"
```

И здесь мы получаем много информации:


| Ввод    | Обозначение                                                                                                        |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `data`      | День недели                                                                                                         |
| `quest`     | Какая сейчас пара                                                                                              |
| `cal`       | Любая переменная с названием`cal`, это означает что это расписание пар |
| `full_date` | Какой тип недели                                                                                                |

Расписание выглядит так:

```python
self.cal = {
    1 : {
        1 : "Биология",
        1.5 : None,
        2 : "Биология",
        2.5 : None,
        3 : "Химия",
        3.5 : None,
        4 : "Оборона Украины Феминистками",
        4.5 : None
    },
    2 : {
        1 : "Оборона Украины",
        1.5 : None,
        2 : "Физра",
        2.5 : None,
        3 : "Английский",
        3.5 : None,
        4 : None,
        4.5 : None
    },
    3 : {
        1 : "Математика",
        1.5 : None,
        2 : "Всемирная история",
        2.5 : "Химия",
        3 : "Математика",
        3.5 : "Зарубежная литература",
        4 : None,
        4.5 : None
    },
    4 : {
        1 : "История Украины",
        1.5 : None,
        2 : "География",
        2.5 : None,
        3 : "Английский",
        3.5 : "Физика",
        4 : "Физика",
        4.5 : None
    },
    5 : {
        1 : "Информатика",
        1.5 : None,
        2 : "Украинский язык",
        2.5 : None,
        3 : "Украинская литература",
        3.5 : None,
        4 : None,
        4.5 : None
    }
}
```

Это словари, первые словари пронумерованы от 1 до 5, это дни недели, а в них тоже номера от 1 до 4 и с промежуточные 1.5, 2.5, 3.5, 4.5 индексом.

Все целые индексы пар, это пары который в числитель, а если есть пара во знаменателе, то надо записать в номер этой пары но с припиской .5.

А если нет ставишь `None` то всё, выведется то что под целим индексом.

А сам код просто так что его объяснение писать не буду.

---

Объяснение данного файла окончено, в [`data.py`](data.py) нет новых вычислений кроме статуса отмены и всё, и там просто используется функции

из [`date_for_now.py`](date_for_now.py), и передает ответы в [`date_for_now.py`](date_for_now.py) что бы вернуть финальный результат, например

результат какая сейчас пара, он использует функцию `para_return` и он передает в неё `data` которую берез из функции `date_return`,

`quest` из `quest_return`, `cal` у него у самого в переменной она есть и она инициилизирована в функции `start`, и `full_date` который

получает из `full_date_return`, а она получает тоже от `start`.

И так все сделано, а сам бот берет из файла [`data.py`](data.py).

Конец. :)
